# SCDeskTools 사용설명서

화면 캡처 · 색상 추출 · 길이/각도 측정을 한 트레이에서 처리하는 데스크톱 유틸리티 모음.

---

## 1. 실행 / 종료 / 트레이

### 실행
- `bin\SCDeskTools.exe` 실행. 실행 시 메인 창이 표시되고 시스템 트레이에 아이콘이 등록된다.
- 개발 편의용 우측 하단 **종료** 버튼이 노출되어 있다 (배포 빌드에서는 제거 예정).

### 메인 창 동작
- **[X] 또는 Alt+F4** — 종료가 아니라 **트레이로 숨김**. 앱은 계속 동작한다.
- **트레이 아이콘 더블클릭** — 메인 창 표시/숨김 토글.
- **트레이 아이콘 우클릭** — 컨텍스트 메뉴 표시.
- **메인 창 우클릭** — 트레이와 동일한 컨텍스트 메뉴 표시.

### 종료
- 트레이 우클릭 → **종료** 또는 메인 창 우측 하단 **종료** 버튼.
- [X] 만으로는 종료되지 않는다.

### 시작프로그램 / Explorer 재시작
- 시작프로그램 등록 후 부팅 시 작업 표시줄(Shell_TrayWnd) 이 늦게 올라와도 자동 복구.
- Explorer 가 크래시 후 재시작되어도 트레이 아이콘이 자동 재등록된다.

---

## 2. 메인 창

기본 즐겨찾기 8 개 툴이 2 열 그리드 버튼으로 배치된다. 각 버튼은 두 줄 캡션이며 1 행은 기능명, 2 행은 글로벌 단축키이다.

| 버튼 | 단축키 |
|---|---|
| 전체 화면 캡처 | Alt+Shift+F |
| 창 캡처 | Alt+Shift+S |
| 영역 캡처 | Alt+Shift+R |
| 클립보드 이미지 띠우기 | Alt+Shift+V |
| 컬러 피커... | Alt+Shift+C |
| 화면 돋보기... | Alt+Shift+M |
| 줄자 | Alt+Shift+L |
| 각도기 | Alt+Shift+P |

---

## 3. 글로벌 단축키 전체

메인 창이 숨겨져 있어도 트레이에 상주 중인 한 어디서든 동작한다.

### 정적 (항상 등록)

| 단축키 | 기능 |
|---|---|
| Alt+Shift+S | 창 캡처 |
| Alt+Shift+F | 전체 화면 캡처 (가상 데스크톱 전체) |
| Alt+Shift+R | 영역 캡처 |
| Alt+Shift+V | 클립보드 이미지 띠우기 |
| Alt+Shift+C | 컬러 피커 |
| Alt+Shift+M | 화면 돋보기 |
| Alt+Shift+P | 각도기 |
| Alt+Shift+L | 줄자 |

### 동적 (모니터 2 개 이상일 때만)

| 단축키 | 기능 |
|---|---|
| Alt+Shift+1 | 1번 모니터 캡처 |
| Alt+Shift+2 | 2번 모니터 캡처 |
| ... | ... 최대 9번 모니터까지 |

모니터 핫플러그·해상도 변경 시 자동으로 재등록된다.

### 충돌 처리
- 다른 앱이 같은 조합을 `RegisterHotKey` 로 먼저 등록한 경우, 시작 시 트레이에서 **풍선 알림**으로 실패한 항목을 알린다.
- 풍선 알림 없이 단축키가 안 먹는다면 다른 앱이 **시스템 키보드 hook** (`WH_KEYBOARD_LL`) 으로 가로챈 것이다 (NVIDIA GeForce Experience, Xbox Game Bar, OBS, Discord 등이 흔한 범인). 해당 앱 종료 또는 그 앱의 단축키 설정 변경으로 우회.

---

## 4. 컨텍스트 메뉴 (트레이 우클릭 / 메인 창 우클릭)

```
표시 / 숨기기
─────────
캡처
  전체 화면 캡처               Alt+Shift+F
  창 캡처                       Alt+Shift+S
  영역 캡처                     Alt+Shift+R
  모니터별 캡처 →               (모니터 2개 이상)
     1번 모니터 캡처            Alt+Shift+1
     2번 모니터 캡처            Alt+Shift+2
     ...
  클립보드 이미지 띠우기        Alt+Shift+V
색상
  컬러 피커...                  Alt+Shift+C
  화면 돋보기...                Alt+Shift+M
측정
  각도기                        Alt+Shift+P
  줄자                          Alt+Shift+L
─────────
정보(&A)...
종료(&X)
```

- 우측에 표시되는 단축키는 글로벌 단축키이므로 메뉴를 열지 않아도 동일한 키로 실행된다.
- "클립보드 이미지 띠우기" 는 클립보드에 이미지가 있을 때만 활성화된다.
- "모니터별 캡처" 서브메뉴는 모니터가 2 개 이상일 때만 표시된다.

---

## 5. 캡처 기능

모든 캡처는 **가상 데스크톱(모든 모니터) 1 회 프리즈** 위에 오버레이를 띄우는 방식으로, 캡처 도중 화면이 흔들리지 않는다.

### 5.1 전체 화면 캡처 (Alt+Shift+F)
- 모든 모니터를 합친 가상 데스크톱 전체를 캡처.
- 결과는 즉시 클립보드에 복사 + 캡처 노트 다이얼로그로 띄움.

### 5.2 창 캡처 (Alt+Shift+S)
- 십자 커서 + 마우스 아래의 top-level window 가 강조된다.
- **좌클릭** = 강조된 창 캡처 / **우클릭, ESC** = 취소.

### 5.3 영역 캡처 (Alt+Shift+R)
- 십자 커서 + 외부 영역 어두운 마스크.
- **좌클릭 드래그** = 사각형 영역 선택 → 떼면 확정.
- **방향키** = 1px 단위 커서 미세 이동 (정밀 위치 조정).
- **우클릭, ESC** = 취소.

### 5.4 모니터별 캡처 (Alt+Shift+1..9)
- 단축키 또는 컨텍스트 메뉴 → 캡처 → 모니터별 캡처 → N번 모니터 캡처.
- 해당 모니터 영역만 즉시 캡처.

### 5.5 클립보드 이미지 띠우기 (Alt+Shift+V)
- 클립보드의 이미지 (또는 텍스트도 이미지화) 를 캡처 노트 다이얼로그로 띄움.
- 클립보드에 이미지/텍스트가 없으면 비활성.

---

## 6. 캡처 노트 다이얼로그 (포스트잇 스타일 결과 창)

캡처 / 클립보드 띠우기 결과는 모두 이 floating dialog 로 떠오른다. 캡션·테두리 없는 popup 이며 작업 표시줄에 노출되지 않는다.

| 조작 | 동작 |
|---|---|
| 일반 드래그 | 창 이동 |
| 가장자리 8 px 안쪽 드래그 | 리사이즈 |
| Shift + 드래그 | 이미지 pan (창 안에서 이미지 위치 조정) |
| 마우스 휠 | 줌 인/아웃 |
| `+`, `-` 키 | 줌 인/아웃 |
| Ctrl + 휠 | 알파(투명도) 조절. 64 ~ 255 범위 (완전 투명 방지) |
| 우클릭 | 컨텍스트 메뉴 (Copy / 100% / Fit / Save / Close) |
| ESC | 닫기 |

여러 노트를 동시에 띄울 수 있다 (각각 독립 lifecycle).

### 저장
- 노트 우상단 닫기 버튼 옆 또는 우클릭 → Save.
- 직전에 사용한 폴더가 기본 위치로 열린다 (다음 저장 시 동일 폴더에서 시작).
- 기본 파일명: `capture_yyyyMMdd_HHmmss.png`.

---

## 7. 색상 도구

### 7.1 컬러 피커 (Alt+Shift+C)
- 모달리스 다이얼로그. 한 번 띄운 뒤 단축키 또는 메뉴로 표시/숨김 토글.
- 선택한 색은 메인 창에 보관되어 다른 도구가 참조 가능.

### 7.2 화면 돋보기 (Alt+Shift+M)
- 마우스 위치 주변의 픽셀을 확대해 정확한 색을 추출.

---

## 8. 측정 도구

모두 가상 데스크톱 프리즈 캡처 위에서 동작 → 측정 중 화면 변화 없음.

### 8.1 줄자 (Alt+Shift+L)
두 점 사이 길이(px) 와 각도(°) 측정.

| 단계 | 조작 |
|---|---|
| 시작 | 좌클릭으로 start 점 → 드래그 → 좌버튼 떼면 end 확정 |
| 편집 | start / end / 라인 자체 (라인 위 = 평행이동) 모두 드래그 가능 |
| Shift | 0° / 45° / 90° 로 각도 잠금 |
| ESC, 우클릭 | 종료 |

### 8.2 각도기 (Alt+Shift+P)
두 라인의 끼인 각도 측정. 3-Phase state machine.

| 단계 | 조작 |
|---|---|
| 1) PlaceArmA | 좌클릭으로 vertex 설정 → 드래그 → 좌버튼 떼면 arm A 확정 |
| 2) PlaceArmB | 마우스 이동만으로 arm B 끝점 따라옴 → 좌클릭으로 arm B 확정 |
| 3) Edit | vertex / arm A end / arm B end 3 개 핸들 드래그로 회전·평행이동 |
| ESC, Enter, 우클릭 | 종료 |

---

## 9. 멀티모니터

- 가상 데스크톱 전체를 한 번에 다루므로 모니터 어디에 마우스가 있든 동일하게 동작.
- 모니터별 캡처: 컨텍스트 메뉴 → 캡처 → 모니터별 캡처 → N번 (또는 Alt+Shift+N).
- DPI 가 모니터마다 달라도 좌표·캡처 영역은 가상 데스크톱 픽셀 좌표 기준으로 일관되게 처리.
- 핫플러그(연결/분리) 또는 해상도 변경 시 모니터 단축키가 자동 재등록.

---

## 10. 자주 묻는 문제

### 단축키 일부가 안 먹는다
1. 시작 시 풍선 알림이 떴는지 확인 — `RegisterHotKey` 충돌이면 알림이 뜬다.
2. 알림 없이 안 먹는다면 다른 앱의 키보드 hook 가로채기. 의심 후보:
   - **NVIDIA GeForce Experience / ShadowPlay** (Alt+Shift+R 가장 흔한 충돌)
   - Xbox Game Bar
   - OBS / Discord / AutoHotkey / PowerToys Keyboard Manager
3. 진단: Debug 빌드 + DebugView 또는 VS 출력 창. `[hotkey]` 로 시작하는 라인에 `RegisterHotKey ... -> OK` 인데 누를 때 `WM_HOTKEY` 가 안 오면 hook 가로채기 확정.
4. NirSoft **HotKeysList** (https://www.nirsoft.net/utils/hotkeys_list.html) 으로 `RegisterHotKey` 로 등록된 hotkey 와 owner process 확인 가능 (단 LL hook 케이스는 못 잡음).

### 메인 창이 안 보인다
- 트레이 아이콘 더블클릭으로 표시.
- 트레이 우클릭 → 표시.
- 직전 종료 시 위치가 화면 밖이면 자동으로 가운데 위치로 복원된다.

### 캡처 결과가 안 뜬다
- 캡처 노트가 화면 밖에 떴을 가능성. 캡처 직전 마우스 위치 또는 캡처된 윈도우의 원래 위치로 뜨므로, 그 모니터를 확인.

### 모니터 단축키 (Alt+Shift+1..9) 가 안 보인다
- 단일 모니터 환경에서는 등록 자체를 생략한다 (전체 화면 캡처와 같은 결과라 단축키 낭비). 외부 모니터를 연결하면 즉시 등록된다 (`WM_DISPLAYCHANGE` 자동 처리).

---

## 11. 향후 추가 예정

`readme.txt` 의 인벤토리에서 우선 후보:
- 마지막 영역 재캡처
- 딜레이 캡처 (3/5/10 s)
- 크로스헤어 가이드
- 노트 위 주석/낙서
- 창 정보 표시 (HWND / 클래스 / 프로세스)
- 즐겨찾기 편집 다이얼로그 + 레지스트리 저장
- 단축키 설정 다이얼로그 + 레지스트리 저장
